apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.globals.labels.app}}
    type: {{ .Values.mysql.labels.type}}
  name: mysql-svc
spec:
  ports:
    - name: "3306"
      port: 3306
      targetPort: 3306
  selector:
    app: {{ .Values.globals.labels.app}}
    type: {{ .Values.mysql.labels.type}}
